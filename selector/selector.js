import { selector } from "recoil";
import { counter } from "../atoms/atoms";

// export const counterValue = selector({
//   key: "counterValue", // unique ID (with respect to other atoms/selectors)
//   get: ({ get }) => {
//     const contor = get(counter);
//     contor=contor+1;
//     return contor;
//   },
// });
